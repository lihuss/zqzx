<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>咨询专区 - 肇中2025</title>
    <link href="/lib/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/lib/fontawesome/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/treehole-style.css">
    <link rel="stylesheet" href="/css/image-grid.css">
    <style>
        :root {
            --consultation-primary: #3b82f6;
            --consultation-dark: #2563eb;
            --consultation-light: #eff6ff;
        }

        .consultation-header::before {
            background: linear-gradient(90deg, var(--consultation-dark), var(--consultation-primary), #60a5fa);
        }

        .btn-consultation {
            background: linear-gradient(135deg, var(--consultation-primary), var(--consultation-dark));
            color: white;
            box-shadow: 0 4px 14px rgba(37, 99, 235, 0.25);
            border: none;
            border-radius: 1rem;
            padding: 0.75rem 1.5rem;
            font-weight: 700;
        }

        .btn-consultation:hover {
            transform: translateY(-1px);
            box-shadow: 0 6px 20px rgba(37, 99, 235, 0.3);
            color: white;
        }

        .nav-tab.active-consultation {
            background: white;
            color: var(--consultation-primary);
            box-shadow: var(--shadow-sm);
        }

        .header-icon-consultation {
            width: 60px;
            height: 60px;
            font-size: 2.5rem;
            background: var(--consultation-light);
            color: var(--consultation-primary);
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1rem;
        }

        .btn-consultation-float {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--consultation-primary), var(--consultation-dark));
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0 4px 14px rgba(37, 99, 235, 0.4);
            z-index: 1000;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .btn-consultation-float:hover {
            transform: scale(1.1);
            box-shadow: 0 6px 20px rgba(37, 99, 235, 0.5);
            color: white;
        }

        /* 知乎风格卡片 */
        .question-card {
            padding: 20px;
            margin-bottom: 0.5rem;
        }

        .question-title {
            font-size: 1.25rem;
            font-weight: 700;
            color: #1f2937;
            line-height: 1.4;
            margin-bottom: 0.5rem;
            display: block;
            text-decoration: none;
        }

        .question-title:hover {
            color: var(--consultation-primary);
        }

        .question-meta {
            color: #6b7280;
            font-size: 0.875rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 0.75rem;
        }

        /* 回答预览区 */
        .answer-preview {
            background: #f8fafc;
            border-radius: 0.75rem;
            padding: 1rem;
            margin-top: 0.75rem;
            border-left: 3px solid var(--consultation-primary);
        }

        .answer-preview-header {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 0.5rem;
        }

        .answer-preview-author {
            font-weight: 600;
            color: #374151;
            font-size: 0.875rem;
        }

        .answer-preview-content {
            color: #4b5563;
            font-size: 0.95rem;
            line-height: 1.6;
            overflow: hidden;
        }

        .answer-preview-content.collapsed {
            max-height: 4.8em;
            position: relative;
        }

        .answer-preview-content.collapsed::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 2em;
            background: linear-gradient(transparent, #f8fafc);
        }

        .answer-preview-content.expanded {
            max-height: none;
        }

        .answer-preview-content.expanded::after {
            display: none;
        }

        .expand-btn {
            background: none;
            border: none;
            color: var(--consultation-primary);
            font-size: 0.875rem;
            font-weight: 600;
            cursor: pointer;
            padding: 0.5rem 0 0 0;
            display: flex;
            align-items: center;
            gap: 0.25rem;
        }

        .expand-btn:hover {
            text-decoration: underline;
        }

        .answer-count-badge {
            background: var(--consultation-light);
            color: var(--consultation-primary);
            padding: 0.25rem 0.5rem;
            border-radius: 0.5rem;
            font-size: 0.75rem;
            font-weight: 600;
        }

        .no-answer-hint {
            color: #9ca3af;
            font-size: 0.875rem;
            margin-top: 0.5rem;
            font-style: italic;
        }
    </style>
</head>

<body>
    <!-- 顶部导航 -->
    <nav class="warm-nav">
        <div class="container">
            <a href="/" class="nav-brand">
                <img src="/images/logo.png" alt="校徽">
                <span class="nav-brand-text d-none d-md-inline">肇中2025</span>
            </a>

            <!-- Tab 切换 -->
            <div class="nav-tabs-wrapper">
                <a href="/treehole" class="nav-tab">
                    <i class="fas fa-tree"></i>
                    <span>树洞</span>
                </a>
                <a href="/consultation" class="nav-tab active-consultation">
                    <i class="fas fa-question-circle"></i>
                    <span>咨询专区</span>
                </a>
                <a href="/hall" class="nav-tab">
                    <i class="fas fa-book-open"></i>
                    <span>回忆录</span>
                </a>
            </div>

            <% if (user) { %>
                <div class="dropdown">
                    <button class="btn-warm-outline dropdown-toggle" type="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user"></i>
                        <span class="d-none d-sm-inline">
                            <%= user.username %>
                        </span>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <span class="dropdown-item-text text-muted small">
                                <%= user.campus %> · <%= user.school_type %> · <%= user.graduation_year %>届<br>
                                            <%= user.class_name %>
                            </span>
                        </li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li>
                            <a class="dropdown-item" href="/profile">
                                <i class="fas fa-id-card me-2"></i>个人主页
                            </a>
                        </li>
                        <% if (user.is_admin) { %>
                            <li><a class="dropdown-item" href="/admin"><i class="fas fa-cog text-danger"></i> 管理面板</a>
                            </li>
                            <% } %>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li>
                                    <form action="/logout" method="POST" class="d-inline">
                                        <button type="submit" class="dropdown-item text-danger">
                                            <i class="fas fa-sign-out-alt"></i> 退出登录
                                        </button>
                                    </form>
                                </li>
                    </ul>
                </div>
                <% } %>
        </div>
    </nav>

    <div class="container py-4">
        <!-- 头部区域 -->
        <div class="treehole-header consultation-header">
            <div class="header-icon-consultation"><i class="fas fa-comments"></i></div>
            <h2>咨询专区</h2>
            <p>求助问答，经验分享，互助成长</p>
        </div>

        <div class="row justify-content-center">
            <!-- 帖子流 -->
            <div class="col-lg-8 col-md-10">
                <!-- 悬浮提问按钮 -->
                <a href="/consultation/new" class="btn-consultation-float" title="我要提问">
                    <i class="fas fa-plus"></i>
                </a>

                <% if (posts.length===0) { %>
                    <div class="treehole-card empty-state">
                        <i class="fas fa-question-circle" style="color: #bfdbfe;"></i>
                        <h5>暂无咨询</h5>
                        <p>有什么问题，发布出来吧！</p>
                    </div>
                    <% } %>

                        <% posts.forEach(function(post) { %>
                            <div class="treehole-card question-card">
                                <!-- 问题标题 (可点击进入详情) -->
                                <a href="/consultation/<%= post.id %>" class="question-title">
                                    <% if (post.type==='question' ) { %>
                                        <span class="badge bg-primary me-1"
                                            style="font-size: 0.7rem; vertical-align: middle;">提问</span>
                                        <% } else if (post.type==='article' ) { %>
                                            <span class="badge bg-info me-1"
                                                style="font-size: 0.7rem; vertical-align: middle;">文章</span>
                                            <% } %>
                                                <%= post.title || (post.content.length> 50 ? post.content.substring(0,
                                                    50) + '...' : post.content) %>
                                </a>

                                <!-- 问题元信息 -->
                                <div class="question-meta">
                                    <span>
                                        <% if (post.is_anonymous) { %>
                                            <i class="fas fa-user-secret me-1"></i>
                                            <%= post.anonymousName || '匿名用户' %>
                                                <% } else { %>
                                                    <i class="fas fa-user me-1"></i>
                                                    <%= post.username %>
                                                        <% } %>
                                    </span>
                                    <% if (post.displayIdentity) { %>
                                        <span class="badge bg-info bg-opacity-10 text-info" style="font-size: 0.7rem;">
                                            <%= post.displayIdentity %>
                                        </span>
                                        <% } %>
                                            <span>
                                                <%= moment(post.created_at).format('MM-DD HH:mm') %>
                                            </span>
                                            <% if (post.type==='question' && post.answerCount> 0) { %>
                                                <span class="answer-count-badge"><i
                                                        class="fas fa-comment-dots me-1"></i>
                                                    <%= post.answerCount %> 个回答
                                                </span>
                                                <% } %>
                                </div>

                                <% if (post.type==='question' ) { %>
                                    <!-- 热门回答预览 (知乎风格) -->
                                    <% if (post.topAnswer) { %>
                                        <div class="answer-preview" id="answer-<%= post.id %>">
                                            <div class="answer-preview-header">
                                                <span class="answer-preview-author">
                                                    <% if (post.topAnswer.is_anonymous) { %>
                                                        <i class="fas fa-user-secret me-1 text-muted"></i>
                                                        <%= post.topAnswer.anonymousName || '匿名用户' %>
                                                            <% } else { %>
                                                                <i class="fas fa-user me-1 text-muted"></i>
                                                                <%= post.topAnswer.username %>
                                                                    <% } %>
                                                </span>
                                                <% if (post.topAnswer.displayIdentity) { %>
                                                    <span class="badge bg-info bg-opacity-10 text-info"
                                                        style="font-size: 0.65rem;">
                                                        <%= post.topAnswer.displayIdentity %>
                                                    </span>
                                                    <% } %>
                                                        <span class="text-primary" style="font-size: 0.75rem;"><span
                                                                style="font-size: 0.6rem;">▲</span>
                                                            <%= post.topAnswer.likes || 0 %>
                                                        </span>
                                            </div>
                                            <div class="answer-preview-content collapsed"
                                                id="answer-content-<%= post.id %>">
                                                <%- post.topAnswer.content.replace(/\n/g, '<br>' ) %>
                                            </div>
                                            <button class="expand-btn" onclick="toggleAnswer(<%= post.id %>)"
                                                id="expand-btn-<%= post.id %>">
                                                <span>展开全文</span>
                                                <i class="fas fa-angle-down"></i>
                                            </button>
                                        </div>
                                        <% } else { %>
                                            <p class="no-answer-hint">暂无回答，<a
                                                    href="/consultation/answer?question_id=<%= post.id %>"
                                                    class="text-primary">来写第一个回答</a></p>
                                            <% } %>
                                                <% } else { %>
                                                    <!-- 文章预览 -->
                                                    <div class="text-secondary mt-2"
                                                        style="font-size: 0.95rem; line-height: 1.6;">
                                                        <% let plainText=post.content.replace(/<[^>]+>/g, '');
                                                            let displayContent = plainText.length > 120 ?
                                                            plainText.substring(0, 120) + '...' : plainText;
                                                            %>
                                                            <%= displayContent %>
                                                                <a href="/consultation/<%= post.id %>"
                                                                    class="text-primary ms-1"
                                                                    style="font-size: 0.875rem; white-space: nowrap;">阅读全文
                                                                    <i class="fas fa-angle-right"></i></a>
                                                    </div>
                                                    <% } %>

                                                        <!-- 操作栏 -->
                                                        <div class="post-actions d-flex justify-content-between align-items-center mt-3 pt-2"
                                                            style="border-top: 1px solid #f1f5f9;">
                                                            <div class="action-left d-flex align-items-center gap-3">
                                                                <% if (post.type==='question' && post.topAnswer) { %>
                                                                    <!-- 问题有top answer时，显示回答的赞同按钮 -->
                                                                    <form
                                                                        action="/consultation/<%= post.topAnswer.id %>/like"
                                                                        method="POST" class="d-inline like-form">
                                                                        <button type="submit"
                                                                            class="btn btn-sm btn-outline-primary border-0 rounded-1 px-2"
                                                                            style="background: #eff6ff; color: #1d4ed8; font-weight: 500;">
                                                                            <span style="font-size: 0.7rem;">▲</span>
                                                                            <span class="ms-1">
                                                                                <%= post.topAnswer.likes> 0 ? '赞同 ' +
                                                                                    post.topAnswer.likes :
                                                                                    '赞同' %>
                                                                            </span>
                                                                        </button>
                                                                    </form>
                                                                    <% } else if (post.type!=='question' ) { %>
                                                                        <!-- 文章可以被赞同 -->
                                                                        <form action="/consultation/<%= post.id %>/like"
                                                                            method="POST" class="d-inline like-form">
                                                                            <button type="submit"
                                                                                class="btn btn-sm btn-outline-primary border-0 rounded-1 px-2"
                                                                                style="background: #eff6ff; color: #1d4ed8; font-weight: 500;">
                                                                                <span
                                                                                    style="font-size: 0.7rem;">▲</span>
                                                                                <span class="ms-1">
                                                                                    <%= post.likes> 0 ? '赞同 ' +
                                                                                        post.likes :
                                                                                        '赞同' %>
                                                                                </span>
                                                                            </button>
                                                                        </form>
                                                                        <% } %>

                                                                            <a href="/consultation/<%= post.id %>"
                                                                                class="btn btn-sm btn-link text-secondary text-decoration-none p-0">
                                                                                <% if (post.type==='question' ) { %>
                                                                                    <i class="fas fa-pen me-1"></i>写回答
                                                                                    <% } else { %>
                                                                                        <i
                                                                                            class="fas fa-comment me-1"></i>查看评论
                                                                                        <% } %>
                                                                            </a>
                                                            </div>
                                                            <div class="action-right">
                                                                <form action="/consultation/<%= post.id %>/report"
                                                                    method="POST" class="d-inline"
                                                                    onsubmit="return confirm('确定要举报该内容吗？')">
                                                                    <button type="submit"
                                                                        class="btn btn-sm btn-link text-muted p-0 border-0 text-decoration-none">
                                                                        <i class="fas fa-flag"></i> 举报
                                                                    </button>
                                                                </form>
                                                            </div>
                                                        </div>
                            </div>
                            <% }); %>
            </div>
        </div>
    </div>

    <!-- 底部 -->
    <footer class="hall-footer">
        <div class="container">
            <p>© 2026 肇中2025届回忆录</p>
        </div>
    </footer>

    <script src="/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script>
        // 展开/收起回答
        function toggleAnswer(postId) {
            const content = document.getElementById('answer-content-' + postId);
            const btn = document.getElementById('expand-btn-' + postId);

            if (content.classList.contains('collapsed')) {
                content.classList.remove('collapsed');
                content.classList.add('expanded');
                btn.innerHTML = '<span>收起</span><i class="fas fa-angle-up"></i>';
            } else {
                content.classList.remove('expanded');
                content.classList.add('collapsed');
                btn.innerHTML = '<span>展开全文</span><i class="fas fa-angle-down"></i>';
            }
        }

        document.addEventListener('DOMContentLoaded', function () {
            document.querySelectorAll('.like-form').forEach(form => {
                form.addEventListener('submit', async function (e) {
                    e.preventDefault();
                    const btn = this.querySelector('button');

                    try {
                        const response = await fetch(this.action, {
                            method: 'POST',
                            headers: {
                                'Accept': 'application/json',
                                'X-Requested-With': 'XMLHttpRequest'
                            }
                        });
                        const data = await response.json();

                        if (data.success) {
                            // Update button text - find the span with ms-1 class
                            const span = btn.querySelector('span.ms-1');
                            if (span) {
                                span.textContent = data.count > 0 ? '赞同 ' + data.count : '赞同';
                            }
                        }
                    } catch (err) {
                        console.error('点赞失败', err);
                    }
                });
            });
        });
    </script>
</body>

</html>

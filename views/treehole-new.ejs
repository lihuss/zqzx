<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>发布新帖 - 树洞</title>
    <link href="/lib/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/lib/fontawesome/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/treehole-style.css">
</head>

<body>
    <!-- 顶部导航 -->
    <nav class="warm-nav">
        <div class="container">
            <a href="/" class="nav-brand">
                <img src="/images/icon.png" alt="校徽">
                <span class="nav-brand-text d-none d-md-inline">东湖畔</span>
            </a>

            <!-- Tab 切换 -->
            <div class="nav-tabs-wrapper">
                <a href="/treehole" class="nav-tab active">
                    <span>树洞</span>
                </a>
                <a href="/consultation" class="nav-tab">
                    <span>咨询</span>
                </a>
                <a href="/market" class="nav-tab">
                    <span>集市</span>
                </a>
                <a href="/hall" class="nav-tab">
                    <span>回忆录</span>
                </a>
            </div>
            <span></span>
        </div>
    </nav>

    <div class="container py-4">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8">
                <div class="treehole-card new-post-card">
                    <div class="new-post-header">
                        <div class="header-icon">🌳</div>
                        <h2>说点什么吧</h2>
                        <p>倾诉心声，匿名发布</p>
                    </div>

                    <form action="/treehole/post" method="POST" enctype="multipart/form-data" class="new-post-form">
                        <div class="mb-4">
                            <textarea name="content" class="form-input-warm large-textarea" rows="8"
                                placeholder="在这里写下你想说的话..." required></textarea>
                        </div>
                        <div class="mb-4">
                            <input type="file" name="image" id="imageUpload" accept="image/*" multiple style="display: none;" onchange="handleFileSelect(this)">
                            
                            <div class="d-flex flex-wrap gap-2" id="upload-grid">
                                <!-- 预览图片将插入到这里 -->
                                
                                <div class="upload-btn-wrapper" id="upload-btn-wrapper" onclick="document.getElementById('imageUpload').click()">
                                    <div class="file-upload-box">
                                        <i class="fas fa-plus"></i>
                                        <span>图片</span>
                                    </div>
                                </div>
                            </div>
                            <small class="text-muted mt-2 d-block" id="upload-hint">支持最多9张，点击图片右上角可删除</small>
                        </div>
                        <button type="submit" class="btn-treehole w-100">
                            <i class="fas fa-paper-plane"></i>
                            <span>发布到树洞</span>
                        </button>
                    </form>

                    <div class="info-tip">
                        <i class="fas fa-user-secret"></i>
                        <span>所有发言均匿名，每个帖子内你会获得一个随机名字</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script>
        const dt = new DataTransfer();

        function handleFileSelect(input) {
            const files = input.files;
            
            // 将新选择的文件添加到 DataTransfer 对象中
            for (let i = 0; i < files.length; i++) {
                // 去重
                let exists = false;
                for(let j = 0; j < dt.files.length; j++) {
                    if (dt.files[j].name === files[i].name && dt.files[j].size === files[i].size) {
                        exists = true;
                        break;
                    }
                }
                if (!exists) {
                    dt.items.add(files[i]);
                }
            }

            // 检查数量限制
            if (dt.files.length > 9) {
                alert('最多只能上传 9 张图片，已保留前 9 张');
                const newDt = new DataTransfer();
                for (let i = 0; i < 9; i++) {
                    newDt.items.add(dt.files[i]);
                }
                dt.items.clear();
                for (let i = 0; i < newDt.files.length; i++) {
                    dt.items.add(newDt.files[i]);
                }
            }

            // 更新 input 的 files
            input.files = dt.files;
            
            // 清空 input value，以便允许重复选择相同文件（如果用户删除了又想加回来）
            // input.value = ''; // 不能清空 value，因为 files 是绑定的。但是为了再次触发 change，可能需要？
            // 实际上 handleFileSelect 是 onchange 触发的，如果 files 属性被我们手动设置了，下次选择同样的，input.value 变了，会触发。
            // 但是为了保险，我们只在 updatePreview 里面不管它。
            
            updatePreview();
        }

        function updatePreview() {
            const grid = document.getElementById('upload-grid');
            const btnWrapper = document.getElementById('upload-btn-wrapper');
            const hint = document.getElementById('upload-hint');

            // 移除所有现有的预览图（在按钮之前的）
            const previews = grid.querySelectorAll('.preview-item');
            previews.forEach(el => el.remove());

            // 隐藏提示如果没有图片
            if (dt.files.length > 0) {
                 hint.textContent = `已选择 ${dt.files.length} / 9 张图片`;
            } else {
                 hint.textContent = '支持最多9张';
            }

            // 生成预览图
            Array.from(dt.files).forEach((file, index) => {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const div = document.createElement('div');
                    div.className = 'preview-item position-relative';
                    
                    const img = document.createElement('img');
                    img.src = e.target.result;
                    img.className = 'border rounded';
                    img.style.width = '100px';
                    img.style.height = '100px';
                    img.style.objectFit = 'cover';
                    
                    const btn = document.createElement('button');
                    btn.type = 'button';
                    btn.className = 'btn btn-danger btn-sm position-absolute top-0 end-0 p-0 d-flex justify-content-center align-items-center';
                    btn.style.width = '20px';
                    btn.style.height = '20px';
                    btn.style.transform = 'translate(30%, -30%)';
                    btn.style.borderRadius = '50%';
                    btn.innerHTML = '&times;';
                    btn.onclick = function(ev) {
                        ev.stopPropagation();
                        removeFile(index);
                    };

                    div.appendChild(img);
                    div.appendChild(btn);
                    
                    // 插入到按钮之前
                    grid.insertBefore(div, btnWrapper);
                }
                reader.readAsDataURL(file);
            });

            // 控制按钮显示
            if (dt.files.length >= 9) {
                btnWrapper.style.display = 'none';
            } else {
                btnWrapper.style.display = 'block';
            }
        }

        function removeFile(index) {
            const newDt = new DataTransfer();
            for (let i = 0; i < dt.files.length; i++) {
                if (i !== index) {
                    newDt.items.add(dt.files[i]);
                }
            }
            dt.items.clear();
            for (let i = 0; i < newDt.files.length; i++) {
                dt.items.add(newDt.files[i]);
            }
            
            document.getElementById('imageUpload').files = dt.files;
            updatePreview();
        }
    </script>
    
    <style>
        .file-upload-box {
            width: 100px;
            height: 100px;
            border: 2px dashed var(--color-border);
            border-radius: 0.75rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            color: var(--text-secondary);
            background-color: var(--color-bg-soft);
            transition: all 0.2s;
        }

        .file-upload-box:hover {
            border-color: var(--color-primary);
            color: var(--color-primary);
            background-color: white;
        }

        .file-upload-box i {
            font-size: 1.5rem;
            margin-bottom: 0.25rem;
        }

        .file-upload-box span {
            font-size: 0.75rem;
        }
        
        /* 覆盖之前的样式影响 */
        .preview-item img {
            width: 100px !important;
            height: 100px !important;
        }
    </style>

    <style>
        .new-post-card {
            padding: 2rem;
        }

        .new-post-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .new-post-header .header-icon {
            font-size: 3rem;
            margin-bottom: 0.5rem;
        }

        .new-post-header h2 {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--text-primary);
            margin-bottom: 0.25rem;
        }

        .new-post-header p {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin: 0;
        }

        .large-textarea {
            min-height: 200px;
            font-size: 1rem;
            line-height: 1.75;
        }

        .image-preview {
            margin-top: 1rem;
            position: relative;
            display: inline-block;
        }

        .image-preview img {
            max-width: 200px;
            max-height: 150px;
            border-radius: 0.75rem;
            object-fit: cover;
        }

        .remove-image {
            position: absolute;
            top: -8px;
            right: -8px;
            width: 24px;
            height: 24px;
            border-radius: 50%;
            background: #ef4444;
            color: white;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.75rem;
        }

        .remove-image:hover {
            background: #dc2626;
        }
    </style>
</body>

</html>